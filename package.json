{
  "name": "peter-zhixue",
  "version": "0.2.0",
  "private": true,
  "description": "Peter Zhixue study bank and server-side AI explanations",
  "scripts": {
    "dev": "node server/dev-server.js",
    "test": "node --test \"tests/*.test.js\"",
    "check": "node --check app.js && node --test \"tests/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  }
}
